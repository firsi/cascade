import { Paragraph } from "grommet";
import styled from "styled-components";

const BackgroundText = styled(Paragraph)`
  font-weight: 700;
  font-size: 300px;
  line-height: 280px;
  position: absolute;
  top: 50px;
  left: 50px;
  opacity: 0.06;
`;

export default BackgroundText;
